spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spendingtracker
    username: lagathub
    password: sketch2002.
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8080